@inherits LayoutComponentBase
@using static BlazorWebApp.Data.Enums
@inject ManagerService M
@inject SDAPIService SDAPI
@inject ComfyUIService CAPI
@inject ThemeService ThemeService
@inject NavigationManager NavManager
@implements IDisposable

<PageTitle>BlazorDiffusion</PageTitle>

<MudThemeProvider @bind-IsDarkMode=M.State.IsDarkMode Theme="_currentTheme" />
<MudDialogProvider />
<MudSnackbarProvider />

<div>
    <MudLayout Style="overflow:hidden;">
        <MudAppBar Style="z-index: 1400;">
            <ProgressContainer />
            <MudIconButton Href="/" Variant="Variant.Text" Icon="fa-solid fa-image me-1" Class="brand w-auto" />
            <NavBar />
            <MudSpacer />
            
            @* Base Selector (ComfyUI only) *@
            @if (M.IsComfyUIUp && _workflowBases != null && _workflowBases.Count > 0)
            {
                <MudSelect Value="M.State.Generation.WorkflowBase" 
                           T="ModelBase" 
                           ValueChanged="HandleWorkflowBaseSelected" 
                           Label="Base" 
                           Variant="Variant.Outlined"
                           Class="base-selector mr-1"
                           AnchorOrigin="Origin.BottomCenter" 
                           TransformOrigin="Origin.TopCenter">
                    @foreach (var workflowBase in _workflowBases)
                    {
                        <MudSelectItem Value="@workflowBase" />
                    }
                </MudSelect>
            }
            
            @* Dark/Light Mode Toggle *@
            <MudIconButton Icon="@(M.State.IsDarkMode ? Icons.Material.Filled.LightMode : Icons.Material.Filled.DarkMode)" 
                           Color="Color.Inherit" 
                           Title="@(M.State.IsDarkMode ? "Switch to Light Mode" : "Switch to Dark Mode")"
                           OnClick="ToggleDarkMode" />
            
            <MudIconButton Class=@(_isModelLoading ? "spinner" : "") Icon="@Icons.Material.Filled.Settings" Color=@(_isModelLoading ? Color.Info : Color.Inherit) Edge="Edge.Start" OnClick="() => DrawerToggler()" />
        </MudAppBar>

        @* Backdrop for TopToolbar *@
        @if (_isToolbarOpen)
        {
            <div class="toolbar-backdrop" @onclick="CloseToolbar"></div>
        }

        <MudExpansionPanels Style="position: fixed; width:100%; z-index:1301; top: 0px;">
            <MudExpansionPanel HideIcon=true @bind-IsExpanded="_isToolbarOpen">
                <CascadingValue Value="@((Action)HandleThemeChanged)">
                    <TopToolbar />
                </CascadingValue>
            </MudExpansionPanel>
        </MudExpansionPanels>
        <MudMainContent>
            <MudContainer Class="mt-5" MaxWidth="MaxWidth.ExtraLarge">
                @Body
            </MudContainer>
        </MudMainContent>
    </MudLayout>
</div>

@code {
    private bool _isToolbarOpen = false;
    private bool _isModelLoading = false;
    private MudTheme _currentTheme = new MudTheme();
    private List<ModelBase> _workflowBases = new();
    
    // Pages that should NOT redirect when Base changes (non-generation pages)
    private static readonly string[] _safePages = { "/", "/prompts", "/resources", "/danbooru" };

    protected override void OnInitialized()
    {
        M.OnProgressChanged += Refresh;
        M.OnSDModelsChange += Refresh;
        M.OnWorkflowBaseChanged += Refresh;
        M.OnComfyUIStateChanged += OnBackendStateChanged;
        M.OnWebuiStateChanged += OnBackendStateChanged;
        LoadTheme();
    }

    protected async override Task OnInitializedAsync() 
    { 
        M.IsWebuiUp = await SDAPI.CheckWebuiState();
        
        // Load backend-dependent resources if a backend is up
        if (M.IsWebuiUp || M.IsComfyUIUp)
        {
            await M.LoadBackendDependentResources();
        }
        
        LoadWorkflowBases();
    }

    private async void OnBackendStateChanged()
    {
        LoadWorkflowBases();
        
        // Load backend-dependent resources when a backend comes online
        if (M.IsWebuiUp || M.IsComfyUIUp)
        {
            await M.LoadBackendDependentResources();
        }
        
        await InvokeAsync(StateHasChanged);
    }

    private void LoadWorkflowBases()
    {
        if (M.IsComfyUIUp && M.State?.Generation?.Workflows != null)
        {
            _workflowBases = M.State.Generation.Workflows
                .Select(w => w.Base.ToString())
                .Where(b => !string.IsNullOrWhiteSpace(b))
                .Distinct(StringComparer.InvariantCultureIgnoreCase)
                .Select(b => Enum.Parse<ModelBase>(b, ignoreCase: true))
                .OrderBy(b => b)
                .ToList();
        }
    }

    private void LoadTheme()
    {
        _currentTheme = ThemeService.GetTheme(M.State.CurrentTheme ?? "Default");
    }

    private void HandleThemeChanged()
    {
        LoadTheme();
        StateHasChanged();
    }

    private async Task ToggleDarkMode()
    {
        M.State.IsDarkMode = !M.State.IsDarkMode;
        await M.SaveState();
        StateHasChanged();
    }

    private async Task HandleWorkflowBaseSelected(ModelBase selected)
    {
        var previousBase = M.State.Generation.WorkflowBase;
        
        M.SetWorkflowBase(selected);
        await M.GetWorkflowModels();
        
        // If base changed and user is on a generation page, redirect to Index
        if (previousBase != selected && !IsOnSafePage())
        {
            NavManager.NavigateTo("/");
        }
    }

    /// <summary>
    /// Checks if the current page is a "safe" page that shouldn't redirect on Base change.
    /// Safe pages: Index, Prompts, Resources, Danbooru
    /// </summary>
    private bool IsOnSafePage()
    {
        var currentPath = NavManager.ToBaseRelativePath(NavManager.Uri);
        var normalizedPath = "/" + currentPath.ToLowerInvariant().TrimEnd('/');
        
        // Check exact matches first
        if (_safePages.Any(p => normalizedPath.Equals(p, StringComparison.OrdinalIgnoreCase)))
            return true;
        
        // Check prefix matches for sub-pages (e.g., /prompts/123)
        if (_safePages.Any(p => p != "/" && normalizedPath.StartsWith(p, StringComparison.OrdinalIgnoreCase)))
            return true;
        
        return false;
    }

    private void DrawerToggler() 
    {
        _isToolbarOpen = !_isToolbarOpen;
        StateHasChanged();
    }

    private void CloseToolbar()
    {
        _isToolbarOpen = false;
        StateHasChanged();
    }

    private async void Refresh()
    {
        _isModelLoading = string.IsNullOrEmpty(M.GetCurrentModel()) || M.GetCurrentModel() == "Loading...";
        LoadWorkflowBases();
        await InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        M.OnProgressChanged -= Refresh;
        M.OnSDModelsChange -= Refresh;
        M.OnWorkflowBaseChanged -= Refresh;
        M.OnComfyUIStateChanged -= OnBackendStateChanged;
        M.OnWebuiStateChanged -= OnBackendStateChanged;
    }
}
